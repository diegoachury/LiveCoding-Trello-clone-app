<% if obj.errors.any? %>
  <div class='alert alert-danger' role='alert'>
    <p><strong>There were some errors in the form:</strong></p>
    <ul>
      <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
